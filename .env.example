# Optional local server port.
PORT=3000

# Server-side AI bouquet image generation. Create a key in Google AI Studio.
# Either variable name is supported; GEMINI_API_KEY takes precedence.
GEMINI_API_KEY=
GOOGLE_API_KEY=

# Optional model override. Defaults to gemini-2.5-flash-image.
GEMINI_IMAGE_MODEL=gemini-2.5-flash-image

# Optional: enables Google Analytics 4 after the visitor grants analytics consent.
VITE_GA_MEASUREMENT_ID=
